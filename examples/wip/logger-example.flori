
import prelude
import fileio
import logger

fn main() {
  l := new_logger()
  add_file(l, stdout)
  add_file(l, open_file("voiceroids.txt", "w"))
  (log l "Yukari\n")
  (log l "Maki\n")
  (log l "Akane\n")
  (log l "Aoi\n")
}

main()

