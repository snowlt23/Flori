
import "core"
import "std/algorithm"

type MyInt {
  x Int
}
fn mi(x Int) MyInt {
  init(MyInt){x}
}

fn cmp_myint(a MyInt, b MyInt) Bool {
  a.x < b.x
}
fn print(mi MyInt) {
  print(mi.x)
}

fn main() {
  # arr := array[5, Int](5, 3, 2, 4, 1)
  # quicksort(arr, cmp_int)
  # for i in range(0, 5-1) {
  #   println(get(arr, i))
  # }
  v := v[MyInt](mi(5), mi(3), mi(2), mi(4), mi(1))
  println(v)
  quicksort(v, cmp_myint)
  println(v)
}

main()
