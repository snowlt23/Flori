
import "core/prelude"
import "core/io"
import "tests/testutils"

expect("12\n")

fn tak(x ^int, y ^int, z ^int) ^int {
  if (x <= y) {
    y
  } else {
    tak(
      tak(x-1, y, z),
      tak(y-1, z, x),
      tak(z-1, x, y))
  }
}

println(tak(12, 6, 0))
